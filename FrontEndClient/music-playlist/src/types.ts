export type PlaylistType = {
    playlistId: number,
    playlistTitle: string,
    imageUrl: string
}

export type SongType = {
    songId: number,
    songTitle : string,
    songAlbum : string,
    songArtist : string,
    songDuration : string
}